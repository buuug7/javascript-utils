export default randomBetween;
/**
 * random a number between max and min
 * @param {number} max
 * @param {number} min
 * @return {number}
 */
declare function randomBetween(max: number, min: number): number;
